name: Linux VPS Setup via Localhost.run SSH

on: [push, workflow_dispatch]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Install SSH
      run: sudo apt update && sudo apt install -y openssh-server

    - name: Enable SSH Server
      run: |
        sudo systemctl start ssh
        sudo systemctl enable ssh
        echo "runner:P@ssw0rd!" | sudo chpasswd
        sudo sed -i 's/^#\?PasswordAuthentication .*/PasswordAuthentication yes/' /etc/ssh/sshd_config
        sudo sed -i 's/^#\?PermitRootLogin .*/PermitRootLogin yes/' /etc/ssh/sshd_config
        sudo systemctl restart ssh

    - name: Create Localhost.run Tunnel
      run: |
        echo "Establishing localhost.run tunnel..."
        ssh -o StrictHostKeyChecking=no -o ServerAliveInterval=60 -N -R 2222:localhost:22 ssh.localhost.run > localhostrun.log 2>&1 &
        sleep 10
        echo "==================== SSH Command ===================="
        echo "ssh -o StrictHostKeyChecking=no runner@ssh.localhost.run -p 2222"
        echo "Password: P@ssw0rd!"
        echo "====================================================="

    - name: Keep job alive for 6 hours
      run: timeout 6h tail -f /dev/null
